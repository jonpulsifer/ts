apiVersion: gateway.networking.k8s.io/v1beta1
kind: Gateway
metadata:
  name: app
spec:
  listeners:
    - name: tls-gateway
      hostname: &hostname request-headers.lolwtf.ca
      port: 443
      tls:
        certificateRefs:
          - kind: Secret
            name: *hostname
